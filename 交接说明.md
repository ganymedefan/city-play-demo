# Citywalk 贴纸人格 H5

设计基准为 1080 × 1920。`index.html` 是独立的四段流程：开始页、贴纸选择页、3 秒 Loading、人格卡片。直接打开外层 `贴纸预览.html` 可离线预览；源码版用本地静态服务打开本目录。

`visual/asset-*.png` 是用户 4x 包中的原始分层资产。七组结果卡四字人格名已烘焙为 `visual/personality-*.png` 透明图，页面不再加载字体；其他可见文字也都是 PNG。`assets/` 是之前完成的 12 张正向单独贴纸。`phone-preview/` 是另一个独立网站，不受本模块影响。

交互保留在 `app.js` 和 `drag.js`：上方贴纸短按揭起并进入槽位，长按 300ms 可拖；下方贴纸长按 300ms 浮起，能换位或拖走。最多三张，扇形框和贴纸一起响应悬停。`results.js` 接管选满后的 Loading、人格分类与卡片关闭动画；点击卡片外或按 Esc 可关闭，没有翻转与可见的 X。结果卡左下按钮重测并回到首页；右下按钮按用户要求是演示按钮，只有按压反馈，不分享或下载。`flow.js` 负责 1080 × 1920 自适应与首页入口。

七类结果人物均已接入：`ride` 骑行、`roam` 地图漫游（36）、`rest` 草地发呆（37）、`draw` 速写记录（38）、`dog` 遛狗（39）、`coffee` 咖啡（42）、`eat` 走哪吃哪（43）。若未来更换人物透明图，可在 `results.js` 的 `art` 对象中替换路径，也可在运行时调用 `PersonalityCard.setArt('coffee', '图片路径')`。`personality.js` 依据所选三张贴纸计算类别；相同组合与点击顺序无关。

Loading 页保留原始 17 号骑车人物图：车轮内部的动态细线、人物轻微起伏与背景网点后移都由 `style.css` 控制，动画只在 3 秒过场中运行，系统偏好减少动态效果时会静止。骑行结果卡直接使用用户提供的 35 号完整构图，保持上方米白色 Logo、红色角标、插画、文字及按钮的原始尺寸；32、33 号素材是两个按钮的视觉资源，按钮点击区域与原图严格重合。其他六类用 22 号空白底图、21、16、30、31 号素材和对应的 `personality-*.png` 动态排版；人物素材对应：36 漫游、37 发呆、38 记录、39 遛狗、42 咖啡、43 吃饭。每次修改后运行 `python3 work/build.py` 重新生成源码版 `index.html` 和离线版 `outputs/贴纸预览.html`。第三方引擎许可保留在 `vendor/`。
